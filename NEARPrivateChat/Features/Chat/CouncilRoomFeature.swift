import Foundation
import SwiftUI

// Council Room feature.
// Self-contained group-chat room view for a council batch: per-model rows with
// stance badges, a synthesis view with agree/dissent/next-step chips, and an
// address-the-room composer. Consumes council data + callbacks. Filled in by
// ring-fenced workstream WS3.
