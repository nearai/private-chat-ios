import Foundation
import SwiftUI

// Briefings + Today feature.
// Self-contained: scheduled recurring prompts (Briefing), a store that persists
// them and their latest result, on-demand runner, and the "Today" Home section
// (Live cards + Scheduled rows). Filled in by ring-fenced workstream WS2.
